/* Reusing the karma transformer without writing a new transformer.

 * Only the command differs in the stage of findandrunkarma rest of the functionality is same 

 * So we are reusing the already available karma transformer code

*/
module.exports = require('./karma.transformer');